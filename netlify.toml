[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  DATABASE_PATH = "/tmp/tenants.db"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["better-sqlite3"]
